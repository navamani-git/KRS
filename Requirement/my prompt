1) Say im in booking screen and during some validation issues while I clicked logout button its scrolls to the validation part only after I relad and clicked logout only working
2) In dashboard screen based on the menu access only the pill box should show - Say for the user aravind it shows lot of bill box that they dont have acces
3) In statement summary The order wise should be received date 
4) Return 

    while return subdealer should select a date when they have returned and in admin approving return they should select a date when they have receiving returned - In transaction it soulould order wise in the date that admin selected return received date

5) While allocate
            - > Allocate date , based on alloate date only the transaction should order by

6) Transaction Statement 
        - > If the descriptin now showing as "Commission - MCPL51500CHK37266" but I need "Commission <br> MCPL51500CHK37266 <br> model <br>

7) Commission approval - Date selection - Commission approval date
        - > In statement this should reflect order by as Commission approval date

8) In Credit / Debit - Both admin and subdealer should have a date selection when the credit debit made
         - > If admin directly giving credit / debit then in transaction order wise would be that date
         -> If subdealer made credit request and admin/finance admin approve by selecting received date then the statement scrrrn should reflect order by as received date

9) Should have an option to restrict to view statement for staff users

10) Spelling correction its ageing

11) In agin screen - Remove the column from showing Purchase Date,Paper Received Date,Invoice Date,	
Insurance Date,Agent Date,Registration Date