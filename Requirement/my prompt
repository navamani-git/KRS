in dealer also i want the parent and child menu structure

while approve returns geting error as " Error: Validation failed: -- ReturnRequestId: Valid return request ID is required Severity: Error"

while approve returns either they can return to their own showrom or for any other sub dealer , So if it reassigned to other sub dealer then it get added to them and goes with approved status they can initiate booking 