@using KRSDealerManagement.Web.Helpers
@using KRSDealerManagement.Web.Models
@{
    var gridId = ViewBag.GridId as string;
    if (string.IsNullOrWhiteSpace(gridId))
    {
        return;
    }

    var options = GridColumnOptions.FromViewBag(ViewData, Context);
    var columns = GridColumnRegistry.Get(gridId, options);

    if (columns.Count == 0)
    {
        return;
    }
}
<partial name="_GridColumnFilters" model="columns" view-data="ViewData" />
